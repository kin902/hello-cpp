#include <iostream>

/*
 * recursion: https://www.youtube.com/watch?v=-TkoO8Z07hI&t=17535s
 */

/*
int factorial(int num);

int main() {
    // recursion = a progreamming techinque where a function invokes itself from within
    //             break a complex concept into a repeatable single step

    int num = 10;
    std::cout << factorial(num) << "\n";
    return 0;
}

int factorial(int num) {
    if(num > 1) {
        return num * factorial(num - 1);
    }
}
//*/
