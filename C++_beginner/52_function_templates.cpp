#include <iostream>

/*
 * function templates: https://www.youtube.com/watch?v=-TkoO8Z07hI&t=17915s
 */

/*
template <typename T>

T max(T x, T y) {
    return (x > y) ? x : y;
}

int main() {
    // function templates: describes what a function looks like
    //                     can be used to generate as many overloaded functions as needed
    //                     each using different data types
    std::cout << max(1, 2) << "\n";

    return 0;
}
//*/
